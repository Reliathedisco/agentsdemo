{
  "name": "demo-slack-triage-agent",
  "version": "1.0.0",
  "description": "Intelligent Slack triage agent — classifies messages, drafts responses, and escalates with human-in-the-loop approval",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --env-file=.env --import tsx src/server.ts",
    "simulate": "node --env-file=.env --import tsx src/simulate.ts"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^1.0.0",
    "@hono/node-server": "^1.19.12",
    "ai": "^4.0.0",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
